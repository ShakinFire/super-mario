import Player from "../traits/player.js";

const handle = ({ entity, level, match, resolver }) => {
    if (entity.traits.has(Player)) {
        // gameContext.entityFactory
    }
};

export const flag = [handle, handle];